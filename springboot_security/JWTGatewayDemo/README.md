# TODO:

This project is going to combine three things

- API gateway that routes requests
- login service that generates JWT tokens
- API gateway would also have the ability to authenticate JWT tokens before routing requests to the appropriate service